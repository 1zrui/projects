# fb1元数据上云跑 — 2026-09-20 02:00

> 摘要：fb1 1790 条名单元数据在云上 4 线程抓取，修了 `COOKIES.exists()` 崩溃坑，跑完结果传回本地。属 [[FB-Reels云部署与QQ群视频推送-2026-09-15]] 流水记录。

- 大哥传来 fb1-cloud-bundle.zip（1790条名单 + 已有99条进度 + cookies私传）。
- 云上实测：proxy 200正常，yt-dlp 2026.08.19可用。
- 踩坑：`fb1_reels_meta_cloud.py` 第62行 `COOKIES.exists()` 必崩（COOKIES是str），已改成 `Path(COOKIES).exists()`，本地源文件也要同步改，不然下次还崩。
- 试水：`python3 fb1_reels_meta_cloud.py 1` 通过，无LOGIN_WALL，新增1条（100条）。
- 全量：已在 `~/fb1/` 挂后台跑（`total=1790 done=100 todo=1690`，4线程），日志 `~/fb1/meta_run.log`，cookies已chmod 600，绝不进git。
- 跑完需把 `~/fb1/fb1_reels_meta.jsonl` 传回本地 D:/Hermes/workspace/projects/fb-reels/。
